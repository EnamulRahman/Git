The working directory is your current project folder – where you’re editing files. Any changes here are unstaged and not tracked yet 

 

Staging Area (Index) 

Changes marked for commit 

 

git commit those staged changes. That creates a snapshot in the repository – the .git directory holds all your commits 
